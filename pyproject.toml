[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/packet_simulator"]

[project]
name = "packet_simulator"
version = "0.0.1"

[project.scripts]
packet-simulator = "packet_simulator.__main__:main"
